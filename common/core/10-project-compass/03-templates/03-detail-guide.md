# 템플릿 3: 상세 GUIDE.md (말단 계층)

> 상위 나침반 [`../03-templates.md`](../03-templates.md) 에서 분리.

## 템플릿 3: 상세 GUIDE.md (말단 계층)

```markdown
# {폴더명}/ 상세 가이드

## 역할

{이 폴더/모듈의 단일 책임을 1-2줄로}

## 파일 목록

| 파일 | 역할 | 주요 export |
|------|------|------------|
| `파일명.ts` | {역할} | `함수명()` — {설명} |
| `파일명2.ts` | {역할} | `ClassName` — {설명} |

## 의존 관계

- **사용하는 모듈**: `{경로}`, `{경로}`
- **사용되는 곳**: `{경로}`, `{경로}`
- **외부 패키지**: `{패키지명}` — {용도}

## 데이터 흐름

```
{입력} → {처리 단계} → {출력}
예: HTTP Request → validateInput() → processData() → Response
```

## 수정 가이드

### 새 {파일/함수/클래스} 추가 시
- [ ] {체크사항 1}
- [ ] {체크사항 2}
- [ ] 이 GUIDE.md 파일 목록 업데이트

### 수정 후 검증
- [ ] `{테스트 명령어}` 실행
- [ ] {관련 기능 수동 확인 방법}

## 주의사항

- ⚠️ {중요 사이드이펙트 또는 제약}
- ⚠️ {수정 시 주의할 전역 영향}

## 환경변수 / 설정

| 변수명 | 용도 | 기본값 |
|--------|------|--------|
| `ENV_VAR` | {용도} | `{기본값}` |
```

---
