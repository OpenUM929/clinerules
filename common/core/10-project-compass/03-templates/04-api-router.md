# 템플릿 4: API/Router 특화 상세 문서

> 상위 나침반 [`../03-templates.md`](../03-templates.md) 에서 분리.

## 템플릿 4: API/Router 특화 상세 문서

```markdown
# {폴더명}/routers 상세 가이드

## 역할

HTTP 엔드포인트 정의 및 요청 라우팅

## 엔드포인트 목록

| 메서드 | 경로 | 파일 | 설명 |
|--------|------|------|------|
| GET | `/api/users` | `users.ts` | 사용자 목록 조회 |
| POST | `/api/users` | `users.ts` | 사용자 생성 |
| GET | `/api/users/:id` | `users.ts` | 단일 사용자 조회 |

## 공통 패턴

- 인증: {미들웨어 경로} 참조
- 에러 처리: {에러 핸들러 경로} 참조
- 유효성 검사: {스키마 경로} 참조

## 수정 가이드

### 새 엔드포인트 추가 시
- [ ] router 파일에 핸들러 추가
- [ ] 스키마/DTO 파일에 입출력 타입 추가
- [ ] 서비스 레이어에 비즈니스 로직 추가
- [ ] 테스트 추가
- [ ] 이 목록 업데이트
```

---
