# 템플릿 E — DB 마이그레이션

> 사용 전 `README.md`의 공통 양식을 먼저 읽는다.
> 공통 섹션(수정 이력, 배경 및 목적, 영향도 분석, 테스트 계획, 리스크)은 템플릿에 중복 기재하지 않는다.

---

## 1. 현재 사용 현황

- **파일에 쓰는 곳**: (제거 대상)
- **파일에서 읽는 곳**: (DB 전환 대상)
- **DB에만 쓰는 곳**: (유지 대상)

## 2. DB 스키마

```sql
-- (테이블 정의. 실제 적용될 DDL)
```

## 3. 마이그레이션 전략

- **1회성 스크립트**: (있을 경우 경로/동작 방식)
- **앱 시작 시 자동 마이그레이션**: (있을 경우 조건/방식)
- **기존 데이터 처리**: (이전 형식 데이터 처리 방식)
- **DEPRECATED 경로**: (이전 경로/파일 처리 방식)

## 4. 상세 구현

### 4.1 서비스 계층

- (함수/메서드 변경 사항)

### 4.2 라우트 계층

- (엔드포인트 변경 사항)
