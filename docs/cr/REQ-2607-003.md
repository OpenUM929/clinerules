## Change Request

- CR ID        : REQ-2607-003
- 레파지토리   : wordcloud (wordcloud_project/)
- 요청 유형    : refactor
- 대상 스코프  : services (perspective_service.py), routes (perspective_routes.py)
- 작업 유형    : 기능 작업
- 요청자       : user
- 요청 날짜    : 2026.07.14

---

### 지침 진행 과정

1. 00-core.md 확인 → "wordcloud 관련 작업"
2. 참조 문서: wordcloud_project/plans/2026/07/14_01_load-scope-slim/14_01_load-scope-slim.md
3. 분석 내용: 그룹분석 다수 경로가 load_all_batches()로 전체 직원 본문(1.9만건) json.loads를 수행. 선택분만 로드하는 경량 함수/SQL 집계로 교체.

---

### 변경 요약 (1줄, 50자 이내)
> 그룹분석 로드 범위 슬림화 (선택분만 로드)

### 변경 배경 / 이유
> 1.9만명 규모에서 불필요한 전체 로드로 메모리/시간 낭비. 필요한 만큼만 로드하도록 변경.

### 변경 전 → 변경 후
- Before: csv-parse/parse-ids/generate/regenerate/users가 load_all_batches() 전체 스캔 후 메타에서 추출
- After : list_all_employee_ids / list_employee_roster / load_employees_batch / load_employee_batch / list_users_with_batch_counts(SQL 집계)로 교체

### 영향 범위
> perspective 라우트 5개 엔드포인트. 동작 보존 검증 완료(OLD==NEW, /users 44건 deep-equal IDENTICAL).

---

### 작업 상세 내역 (파일별)

| 순번 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| 1 | `wordcloud_project/src/services/perspective_service.py` | 수정 | `load_employees_batch()`, `list_employee_roster()`, `list_users_with_batch_counts()` 신규(IN 조회/SQL 집계) |
| 2 | `wordcloud_project/src/routes/perspective_routes.py` | 수정 | csv-parse/parse-ids/generate/regenerate/users 라우트를 위 함수들로 교체 |

**변경 코드 요약:**
```python
# Before
unified = load_all_batches()
for er in unified.get('employee_results', []):
    meta = er.get('metadata', {})
    eid = meta.get('target_employee_id')

# After
roster = list_employee_roster()
for row in roster:
    eid = row['employee_id']
```

---

### FP (Function Point) 산정 기준

| 유형 | 항목 | 복잡도 | FP | 설명 |
|------|------|--------|-----|------|
| EO | 선택 직원 매트릭스 생성 | 보통 | 3 | load_employees_batch IN 조회 |
| EQ | 명단/사용자 집계 조회 | 낮음 | 1 | SQL GROUP BY |
| ILF | 배치 내부 데이터 유지 | 낮음 | 1 | unified dict 구조 보존 |

### 커밋 메시지 작성 시 적용
```
# 기능 작업 (feat, refactor)
REQ-2607-003: 그룹분석 로드 범위 슬림화 (선택분만 로드)

- load_employees_batch(employee_ids) 신규: IN 조회로 선택 직원만 로드
- list_employee_roster()/list_users_with_batch_counts() SQL 집계 신규
- csv-parse/parse-ids/generate/regenerate/users 라우트 교체(본문 json.loads 미실행)

Refs: REQ-2607-003
FP: 5 (EI:0, EO:3, EQ:1, ILF:1, EIF:0) | 공수: 6H | 작업유형: 기능 (refactor)
```
