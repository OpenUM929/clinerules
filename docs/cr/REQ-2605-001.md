## Change Request

- CR ID        : REQ-2605-001
- 레파지토리   : msys (D:\dev\msys)
- 요청 유형    : fix
- 대상 스코프  : Backend(dao/service/routes/mapper/utils), Frontend(static/js/templates/css), Database(DDL/SQL)
- 작업 유형    : 기능 작업
- 요청자       : etl-user
- 요청 날짜    : 2026.05.06

---

## 📊 현재 상태
> 주석 제거 작업(remove_comments_safe.py 수행) 후 코드 구조 변경으로 인한 버그 발생
> - 주석 제거 과정에서 불필요한 코드 라인도 함께 삭제되어 기능 오작동 발생
> - DB 스키마 변경(DDL) 및 SQL 쿼리 수정 필요
> - 프론트엔드 JS 모듈 및 템플릿 렌더링 오류 수정 필요

## ✅ 완료된 것
> - [x] 전체 프로젝트 주석 제거 작업 수행
> - [x] 주석 제거 후 발생한 백엔드 버그 수정 (dao, service, routes, mapper, utils)
> - [x] 프론트엔드 JS 모듈 버그 수정 (static/js 전반)
> - [x] DB 스키마(DDL) 및 SQL 쿼리 수정
> - [x] 템플릿 및 CSS 수정
> - [x] 신규 모듈 추가 (userAccessInfo 탭: config.js, statusManager.js)

## 🔄 진행 중인 것
> - [ ] 없음 (모든 수정 완료)

## ❓ 미결 결정사항
> 없음

## 📋 다음 작업
> - [ ] 배포 후 사용자 확인 테스트
> - [ ] 주석 제거 도구(remove_comments_safe.py) 개선 필요 시 피드백 반영

## ⚠️ 주의사항 / 이전 실수
> - 주석 제거 시 코드 로직까지 삭제되지 않도록 주의 필요
> - remove_comments_safe.py 도구 사용 시 테스트 필수
> - 대규모 변경 시 백엔드/프론트엔드/DB 일관성 확인 필수

## 📁 관련 파일 경로
| 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| Database | `DDL/*.sql` | 수정 | DB 테이블 스키마, 초기 데이터, 컬럼 매핑 변경 (22개 파일) |
| Backend | `dao/*.py` | 수정 | 데이터 접근 레이어 로직 수정 (12개 파일) |
| Backend | `service/*.py` | 수정 | 비즈니스 로직, 서비스 함수 수정 (18개 파일) |
| Backend | `routes/*.py` | 수정 | API 라우팅, 요청 처리 로직 수정 (12개 파일) |
| Backend | `mapper/*.py` | 수정 | 데이터 매핑 로직 수정 (6개 파일) |
| Backend | `utils/*.py` | 수정 | 공통 유틸리티 함수 수정 (5개 파일) |
| Frontend | `static/js/**/*.js` | 수정 | 프론트엔드 모듈, 페이지별 JS 로직 수정 (50+ 파일) |
| Frontend | `templates/*.html` | 수정 | HTML 템플릿, UI 구조 변경 (20+ 파일) |
| Frontend | `static/css/*.css` | 수정 | 스타일, 레이아웃 변경 (5개 파일) |
| Database | `sql/*.sql` | 수정 | DB 쿼리, 스케줄 설정 SQL 변경 (15+ 파일) |
| Frontend | `static/js/tabs/userAccessInfo/config.js` | 생성 | 사용자 접근 정보 탭 신규 설정 모듈 |
| Frontend | `static/js/tabs/userAccessInfo/statusManager.js` | 생성 | 사용자 접근 정보 상태 관리 모듈 |

## 📝 상세 변경 내용

### 변경 요약
- 주석 제거 후 발생한 백엔드/프론트엔드 버그 전반 수정
- DB 스키마(DDL) 및 SQL 쿼리 정리
- 사용자 접근 정보 탭 신규 모듈 추가

### Before / After (예시)

**파일: `dao/api_key_mngr_dao.py`**
```python
# Before (주석 제거 후 일부 로직 누락된 상태)
def get_api_key_list():
    # TODO: fix needed
    pass

# After (정상 동작하도록 복구)
def get_api_key_list():
    return db.session.query(ApiKey).all()
```

**파일: `static/js/tabs/userAccessInfo/config.js` (신규)**
```javascript
// After (신규 생성)
export const config = {
    apiEndpoint: '/api/user/access',
    refreshInterval: 30000
};
```

---

## FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 (fix) |
| 기능 점수 (FP) | 10 (복잡: 230+ 파일, 다중 모듈 대규모 변경) |
| 예상 공수 | 88H (10 FP × 8.8H) |

---

## 📢 공지용 요약

**[상세 내용]**
> 주석 제거 작업 후 발생한 전반적인 버그 수정 작업. 백엔드(dao/service/routes/mapper/utils), 프론트엔드(JS/템플릿/CSS), DB(DDL/SQL) 전 계층 230+ 파일 수정. 사용자 접근 정보 탭 신규 모듈 추가.

**[요약]**
> 주석 제거 후 버그 수정 (230+ 파일)
