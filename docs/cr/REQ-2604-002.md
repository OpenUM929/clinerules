## Change Request

- CR ID        : REQ-2604-002
- 레파지토리   : wordcloud_project
- 요청 유형    : refactor
- 대상 스코프  : models, services, modules
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.04.22

---

### 지침 진행 과정

1. 00-core.md 확인 → "프로젝트 분석"
2. 참조 문서: sql.txt
3. 분석 내용: 메타데이터 구조와 실제 구현 비교

---

### 변경 요약 (1줄, 50자 이내)
> 메타데이터에 evaluator_id, hierarchy_lever 필드 추가

### 변경 배경 / 이유
- sql.txt(발명신고서)에 정의된 누락 필드 추가 필요
- evaluator_id: 평가자 통계, 편향 분석에 필수
- hierarchy_level: 계층별 평가 경향 분석 필요

### 변경 전 → 변경 후
- Before: evaluator_id 없이 부서/직급만으로 평가자 추적 불가능
- After : evaluator_id, evaluator_hierarchy_level 필드로 평가자별/계층별 분석 가능

### 영향 범위
> 메타데이터 생성 로직, 통합 분석 aggregation

---

### 작업 상세 내역 (파일별)

| 순번 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| 1 | `src/models/metadata_manager.py` | 수정 | create_employee_metadata에 evaluation_id, evaluator_id, hierarchy_level 필드 추가 |
| 2 | `src/services/batch_processor.py` | 수정 | group_data_by_employee에서 evaluator_id, evaluator_hierarchy_level 자동 생성 |
| 3 | `src/modules/metadata_analysis.py` | 수정 | evaluator_analysis에 hierarchy_level_distribution 추가 |

**변경 코드 요약:**

```python
# metadata_manager.py - evaluation_id 자동 생성
for idx, evaluation in enumerate(evaluations):
    if 'evaluation_id' not in analyzed_eval:
        analyzed_eval['evaluation_id'] = f"eval-{employee_id}-{idx+1}-{date}"

# batch_processor.py - evaluator_hierarchy_level 기본값
if 'evaluator_hierarchy_level' not in evaluation:
    if any(p in position for p in ['과장', '팀장', 'manager']):
        evaluation['evaluator_hierarchy_level'] = 'manager'
    else:
        evaluation['evaluator_hierarchy_level'] = 'staff'

# metadata_analysis.py - 계층별 분포 집계
if 'evaluator_hierarchy_level' in evaluation:
    level = evaluation['evaluator_hierarchy_level']
    hierarchy_level_distribution[level] += 1
```

---

### FP / 작업 구분

| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 |
| 기능 점수 (FP) | 3 |
| 예상 공수 | 0.5NH |

---

### 공지용 요약

**[상세 내용]**
> sql.txt 정의에 따라 메타데이터에 evaluator_id, evaluator_hierarchy_level 필드를 추가했습니다. CSV 매핑 시 evaluator_id가 없을 경우 evaluation_date에서 자동 생성하고, evaluator_hierarchy_level은 evaluator_position(과장, 팀장 등)에 따라 manager/staff으로 자동 설정합니다. 통합 분석 시 evaluator_analysis에 hierarchy_level_distribution을 집계합니다.

**[요약]**
> feat: evaluator_id 및 계층 레벨 필드 추가