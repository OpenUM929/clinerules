## Change Request

- CR ID        : REQ-2605-003
- 레파지토리   : D:\dev\msys
- 요청 유형    : feat + fix
- 대상 스코프  : templates, static/js/modules/api_key_mngr
- 작업 유형    : 기능 작업
- 요청자       : 사용자
- 요청 날짜    : 2026.05.06

---

## 현재 상태
> 일괄 수정 팝업에서 사용자 편의성 개선 및 Shift+클릭, 라벨 토글 버그 수정 필요

---

## ✅ 완료된 것
- [x] 일괄 수정 팝업 등록일 기본값(오늘), 기간 기본값(1년) 설정
- [x] 이메일 select/input 콤보 UI 구현 (선택된 데이터 이메일 중 선택 또는 새로 입력)
- [x] 팝업 너비 750px 고정 (inline style)
- [x] Shift+클릭 시 체크박스 클릭 동작 수정 (3개 테이블)
- [x] 팝업 내 라벨 클릭 시 체크박스 토글 (`for` 속성 추가)

---

## FP / 작업 구분
| 항목 | 내용 |
|------|------|
| 작업 유형 | 기능 작업 (feat + fix) |
| 기능 점수 (FP) | 7 |
| 예상 공수 | 61.6H |

---

## 📁 관련 파일 경로
| 구분 | 파일 경로 | 변경 유형 | 상세 내용 |
|------|-----------|-----------|-----------|
| Frontend | `templates/api_key_mngr.html` | 수정 | 팝업 구조, 이메일 UI, 라벨 for 속성 3개 추가 |
| Frontend | `static/js/modules/api_key_mngr/core.js` | 수정 | 모달 로직, 이메일 처리, 기본값 설정 |
| Frontend | `static/js/modules/api_key_mngr/table.js` | 수정 | Shift+클릭, 체크박스 이벤트 처리 |

---

## 📝 상세 변경 내용

### Before / After

**1. 일괄 수정 팝업 기본값**
```javascript
// Before
document.getElementById('batchEditStartDt').value = '';
document.getElementById('batchEditDue').value = '';

// After
const today = new Date().toISOString().split('T')[0];
document.getElementById('batchEditStartDt').value = today;
document.getElementById('batchEditDue').value = '1';
```

**2. 이메일 select/input 구조**
```html
<!-- Before -->
<textarea id="batchEditEmail"></textarea>

<!-- After -->
<select id="batchEditEmailSelect">...</select>
<input type="email" id="batchEditEmailInput" class="hidden">
<span id="batchEditEmailNoData" class="hidden">선택된 항목에 이메일이 없습니다.</span>
```

**3. 팝업 너비 고정**
```html
<!-- Before -->
<div class="... w-[750px] min-w-[750px] ...">

<!-- After -->
<div class="..." style="width: 750px; min-width: 750px; max-width: 750px;">
```

**4. 라벨 클릭 토글**
```html
<!-- Before -->
<label class="...">기간 (년)</label>

<!-- After -->
<label for="batchEditCheckDue" class="...">기간 (년)</label>
```

---

## 📢 공지용 요약

**[상세 내용]**
> 일괄 수정 팝업의 사용자 경험 개선 및 버그 수정

**[요약]**
> 팝업 기본값 설정, 이메일 UI 개선, 너비 고정, Shift+클릭 및 라벨 토글 수정

---

Refs: REQ-2605-003
FP: 7 | 공수: 61.6H | 작업유형: 기능 (feat+fix)
