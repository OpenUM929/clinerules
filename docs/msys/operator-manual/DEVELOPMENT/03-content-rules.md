# 03. 공통 작성 규칙 · 관리자 설정 특이사항

> 나침반: [../DEVELOPMENT.md](../DEVELOPMENT.md)

---

## 1. 공통 작성 규칙

### 1.1 한글 작성 원칙
- 모든 문서는 한글로 작성.
- 영문 용어는 괄호 병기: "대시보드(Dashboard)".
- 메뉴ID는 영문 그대로: `mngr_sett`, `api_key_mngr`.

### 1.2 테이블 작성 규칙
```markdown
| 컬럼1 | 컬럼2 | 컬럼3 |
|-------|-------|-------|
| 값1   | 값2   | 값3   |
```
- 표가 한 페이지보다 길면 논리적 지점에서 **여러 표로 나누고 머리행(헤더)을 반복**한 뒤 사이에 `---pb---`([00-a4-authoring-guide.md](00-a4-authoring-guide.md) §3-2). 표는 `table-layout: fixed` 라 폭이 균등 분배된다.
- 정렬은 구분선에 `:--:`(가운데)/`--:`(오른쪽)/`---`(왼쪽) 로 명시(00 §3-5).

### 1.3 경고/주의사항 강조
```markdown
> ⚠️ **주의**: 이 작업은 되돌릴 수 없습니다.
> 💡 **팁**: 단축키 `Ctrl+F`를 사용하세요.
> 🔴 **위험**: 삭제된 데이터는 복구할 수 없습니다.
```

### 1.4 개조식(목록)
- `□`/`○` 문자를 직접 찍지 말고 **마크다운 목록**(`- 항목`, 중첩 `  - 항목`)을 쓴다. `reportTheme` 가 □/○ 마커를 그린다(00 §3-5). (문자를 직접 찍으면 항목마다 별개 블록이 되어 페이지 중간에서 쪼개지고 마커가 이중으로 보인다.)

---

## 2. 관리자 설정(mngr_sett) 작성 특이사항

`05-mngr-sett.md`는 정보가 방대하므로 별도 파일(탭 하위파일 방식, [05-composition-nav.md](05-composition-nav.md))로 관리한다.

### 2.1 추가 섹션 구조
| 섹션 | 내용 |
|------|------|
| 10. API 관리 | API 키 등록/수정/조회, 만료일 관리, 메일 테스트 |

### 2.2 API 관리 상세
- **10.1 API 키 목록 조회**: 페이징·검색, 코드/만료일/담당자 확인.
- **10.2 API 키 등록/수정**: 코드·만료일·담당자 이메일 입력, 저장 시 유효성 검사.
- **10.3 API 키 삭제**: 선택 삭제, 일괄 삭제.
- **10.4 만료 알림 설정**: 30일 전/7일 전/당일 메일 발송, 메일 스케줄.
- **10.5 메일 테스트**: 테스트 메일 발송, SMTP 연결 확인.

---

## 3. 용어·상태코드 사전 (운영자 필수)

> 운영자는 개발자가 아니다. 문서에 처음 등장하는 **전문용어·코드**(예: `Job ID`, `cron`, 상태코드 `CD901~`)는 **반드시 쉬운 말로 풀이**한다.

- **위치**: 코드가 적은 문서는 처음 등장 지점에 괄호 풀이. 코드/약어가 많은 문서(대시보드·수집스케줄 등)는 하단에 `## 용어·코드 사전` 표를 둔다.
- **상태코드 표 필수 항목**: `코드 | 의미 | 운영자 관점 설명`. 예:

| 코드 | 의미 | 운영자 관점 |
|------|------|------------|
| `CD901` | 정상 수집 | 정상 — 조치 불필요 |
| `CD902` | 장애 발생 | 실패 — 원인 확인·재수집 검토 |
| `CD903` | 미수집 | 데이터 없음 — 스케줄/원천 점검 |
| `CD904` | 수집중 | 진행 중 — 잠시 후 재확인 |

- 공통 코드/약어는 `appendix/`에 사전을 두고 각 문서에서 링크할 수 있다.
